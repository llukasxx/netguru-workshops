.panel.panel-warning
  .panel-heading.bolder Listing categories
  .panel-body
    %p Click show on each category to see it's products, or click below to see all products
    = link_to "All products", category_products_path(1), class: "btn btn-warning", role: "button"
      
  %table.table
    %tr
      %th Name
      %th Total products
      %th Action

    - categories.each do |category|
      %tr
        %td= category.name
        %td= category.products.count
        %td
          = link_to 'Show', category, class: "btn btn-success btn-sm", role: "button"
          -if is_admin?(current_user) 
            = link_to 'Edit', edit_category_path(category), class: "btn btn-warning btn-sm", role: "button"
            = link_to 'Destroy', category, method: :delete, data: { confirm: 'Are you sure?' }, class: "btn btn-danger btn-sm", role: "button"

%br
-if is_admin?(current_user)
  = link_to 'New Category', new_category_path, class: "btn btn-lg btn-info", role: "button"
